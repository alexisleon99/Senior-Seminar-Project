<p id="notice"><%= notice %></p>
<div class="col-lg-12 text-center">
  <h1 class="mt-5"> Travel Form</h1>
</div>
  <table>
  <tbody>
    <% @travel_forms.each do |travel_form| %>
    <div>
      <tr>
      <td> Travel form </td>
        <td><%= travel_form.id %></td>
        <td><%= link_to 'Review', travel_form %></td>
      </tr>
    </div>
    <% end %>
  </tbody>
</table>
<%= link_to 'Back', employee_pages_path %>
