<%= form_with(model: employee_page, local: true) do |form| %>
  <% if employee_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_page.errors.count, "error") %> prohibited this employee_page from being saved:</h2>

      <ul>
      <% employee_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
