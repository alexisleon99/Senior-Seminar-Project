<%= form_with(model: payment_manager, local: true) do |form| %>
  <% if payment_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment_manager.errors.count, "error") %> prohibited this payment_manager from being saved:</h2>

      <ul>
      <% payment_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
