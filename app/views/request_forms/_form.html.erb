
<div class="nested-fields">
<%= form_with(model: request_form, local: true) do |form| %>
  <% if request_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_form.errors.count, "error") %> prohibited this request_form from being saved:</h2>

      <ul>
      <% request_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

  <div class="field">
  <%= form.collection_select(:departments_id, Department.all, :id, :names, { :prompt => 'Select a Department', :selected => @request_form.departments_id }, { class: 'form-control' })%>
  </div>

<div class="row">
  <div class="col-md-4">
    <%= form.text_field :amount, placeholder: "Amount"%>
  </div>
</div>

</br>
<div class="row">
  <div class="col-md-4">
    <%= form.text_field :travel_form_id, placeholder: "Travel Form Id"%>
  </div>
</div>
</br>
  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>

</div>

